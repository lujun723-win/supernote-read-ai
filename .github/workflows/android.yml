name: Android CI

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build DeepSeek APK
      run: ./gradlew assembleDeepseek

    - name: Run DeepSeek Unit Tests
      run: ./gradlew testDeepseekUnitTest

    - name: Upload DeepSeek APK
      uses: actions/upload-artifact@v4
      with:
        name: ReadAssist-deepseek
        path: app/build/outputs/apk/deepseek/ReadAssist-v1.9.7-deepseek-deepseek.apk
